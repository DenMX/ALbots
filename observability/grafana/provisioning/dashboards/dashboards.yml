apiVersion: 1

providers:
  - name: ALbots
    orgId: 1
    folder: ALbots
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
